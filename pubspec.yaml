name: plan_b
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  # 환경 변수 관리 (.env)
  flutter_dotenv: ^5.1.0
  # API 통신 (Gemini, Places API)
  http: ^1.1.0
  # 위치 정보 및 거리 계산
  geolocator: ^10.1.0
  geocoding: ^2.1.1
  # 데이터 로컬 저장 및 오프라인 모드 캐싱
  shared_preferences: ^2.2.2
  # [추가] 지도 보기 기능 구현용
  google_maps_flutter: ^2.5.0
  # 기타 유틸리티
  csv: ^6.0.0
  path_provider: ^2.1.2
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - .env